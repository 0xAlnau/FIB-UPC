#include<iostream>
#include<set>
using namespace std;

int main() {
    set<string> alone;
    set<pair<string,string>> couples;

    couples.insert({"Laura", "Arnau"});
    couples.insert({"Paula", "Jonathan"});
    string action, name1, name2;
    while (cin >> action) {
        if (action == "affair") {
            cin >> name1 >> name2;



        }

        else { // "info"

        }
    }
}
