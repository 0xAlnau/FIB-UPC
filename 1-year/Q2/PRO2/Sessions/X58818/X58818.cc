#include<iostream>
using namespace std;

void arbre(int n, int& offset){
    if (n == 0) {

    }
    else {
        if (n % 2 == 0) { //par

        }
        else { //impar

        }
    }
}

int main(){
    int cas_n;
    while (cin >> cas_n) {
        arbre(cas_n, 0);
    }
}
